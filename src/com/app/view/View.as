// com.app.view.View// Adam Riggs//package com.app.view {	import com.adam.debug.DebugWindow;	import com.adam.events.MuleEvent;	import com.adam.utils.Utils;	import com.app.controller.LeapController;	import com.app.model.Model;		import flash.display.Sprite;	import flash.events.Event;	import flash.events.MouseEvent;
		public class View extends Sprite{				//vars				//objects		private var utils:Utils=Utils.instance;		protected var model:Model=Model.instance;		protected var _lc:LeapController;		protected var _ls:LeapStatistics;				protected var _debugWindow:DebugWindow;				//const		public const NAME:String="view";		public const RETURNTYPE:String=NAME;						/** Storage for the singleton instance. */		private static const _instance:View = new View(ViewLock);				public function View(lock:Class){			// Verify that the lock is the correct class reference.			if (lock != ViewLock)			{				throw new Error("Invalid View access.  Use View.instance instead.");			} else {				//init();							}		}		//*****Initialization Routines				public function init():void{			debug("init()");						if(stage){								initVars();				initEvents();				initObjs();			} else {				addEventListener(Event.ADDED_TO_STAGE, onAddedToStage, false, 0, true);			}		}				private function initVars():void{					}				private function initEvents():void{			utils.em.listen(NAME, onView);			addEventListener(Event.ENTER_FRAME, onEnterFrame, false, 0, true);		}				private function initObjs():void{						_lc=LeapController.instance;						_ls=LeapStatistics.instance;			addChild(_ls);						_debugWindow = DebugWindow.instance;			addChild(_debugWindow);					}		//*****Core Functionality						//*****Event Handlers				public function onClick(e:MouseEvent):void{					}				protected function onEnterFrame(e:Event):void{			_ls.update();						//radio on//			if(_lc.getPoke()){//				_acura.radioOn();//			}//			//			//volume//			if(_lc.getSwipeLeft3() || _lc.getSwipeRight3()){//				_acura.startVolumeControl();//			}//			//			if(_lc.fingerCount==0){//				_acura.endVolumeControl();//			}//			//			if(_acura.volumeControl==true){//				//volume control gets precidence //				//				//debug("_acura.volumeControl=="+_acura.volumeControl);//				//debug("_lc.palmY=="+_lc.palmY);//				_acura.setVolume(_lc.palmY);//			} else {//				//				//switch song//				if(_lc.getSwipeLeft2() || _lc.getSwipeRight2()){//					_acura.switchSong();//				}//				//				//wipers//				if(_lc.getFingerWag() || _lc.getSwipeLeft1() || _lc.getSwipeRight1()){//					_acura.toggleWipers();//				}//			}					}				protected function onAddedToStage(e:Event):void{			removeEventListener(Event.ADDED_TO_STAGE, onAddedToStage);			initVars();			initEvents();			initObjs();		}						private function onView(e:MuleEvent):void{			/*debug("onView()");			debug("e.data.sender=="+e.data.sender);			debug("e.data.type=="+e.data.type);*/			switch(e.data.type){																default:					debug("onView()");					debug("*type not found");					debug("e.data.sender=="+e.data.sender);					debug("e.data.type=="+e.data.type);					break;							}		}				//*****Gets and Sets				public static function get instance():View{return _instance;}		//*****Utility Functions				//**visibility		public function show():void{			this.visible = true;		}				public function hide():void{			this.visible = false;		}				//**debug		private function debug(str:String):void{			utils.debug(NAME,str);		}				}//end class}//end packageclass ViewLock{}